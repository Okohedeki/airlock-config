airlock_config: "0.5"
agent:
  name: hello-agent
  version: 0.1.0
  description: The smallest legal Airlock Config contract — a single skill with no rules.
category:
  industry: other
  capability: other
skills:
  - id: ping
    description: Liveness check.
    input:
      type: object
    output:
      type: object
      properties:
        pong:
          type: boolean
